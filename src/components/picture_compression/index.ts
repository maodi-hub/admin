import pictureCompression  from "./src/picture_compression.vue";

import { withInstall } from "@/utils";

const 
  PictureCompression = withInstall(pictureCompression);

  export {
    PictureCompression
  }
